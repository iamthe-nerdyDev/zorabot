import type { Metadata, Viewport } from 'next';
import { IBM_Plex_Mono, IBM_Plex_Sans_Condensed } from 'next/font/google';
import GlobalProvider from '@/components/providers/GlobalProvider';
import NextTopLoader from 'nextjs-toploader';
import 'react-loading-skeleton/dist/skeleton.css';
import './globals.css';
import { Toaster } from '@/components/ui/sonner';

const ibmPlexSans = IBM_Plex_Sans_Condensed({
  variable: '--font-ibm-plex-sans',
  subsets: ['latin'],
  weight: ['100', '200', '300', '400', '500', '600', '700'],
});

const ibmPlexMono = IBM_Plex_Mono({
  variable: '--font-ibm-plex-mono',
  subsets: ['latin'],
  weight: ['100', '200', '300', '400', '500', '600', '700'],
});

const FarcasterEmbedJson = {
  version: '1',
  imageUrl: 'https://zorabot.vercel.app/farcaster/icon.png',
  button: {
    title: 'Launch ZoraCore',
    action: {
      type: 'launch_miniapp',
      name: 'ZoraCore',
      url: 'https://zorabot.vercel.app',
      splashImageUrl: 'https://zorabot.vercel.app/farcaster/splash.png',
      splashBackgroundColor: '#0a0a0a',
    },
  },
};

export const metadata: Metadata = {
  description: 'Generated by create next app',
  title: {
    default: 'ZoraCore',
    template: '%s | ZoraCore',
  },
  openGraph: {
    title: {
      default: 'ZoraCore',
      template: '%s | ZoraCore',
    },
  },
  other: {
    'fc:miniapp': `${JSON.stringify(FarcasterEmbedJson)}`,
    'fc:frame': `${JSON.stringify(FarcasterEmbedJson)}`,
  },
};

export const viewport: Viewport = {
  userScalable: false,
  themeColor: '#0a0a0a',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${ibmPlexSans.variable} ${ibmPlexMono.variable} antialiased`}>
        <GlobalProvider>{children}</GlobalProvider>
        <NextTopLoader color="#05df72" />
        <Toaster position="bottom-right" />
      </body>
    </html>
  );
}
